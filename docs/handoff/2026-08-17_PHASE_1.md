# Phase 1 handoff

Date: 2026-08-17
Agent: Codex
Checkpoint: Phase 1 — property-to-Vision vertical slice
Branch: `agent/tract-autonomous-build-20260817`
Working tree before: Phase 0 commit `bfd91a5`

## Summary

- Added seven original, AI-generated Florida property planning images and seven clearly synthetic fixture records.
- Rebuilt `/properties`, added stable `/properties/[listingKey]` detail routes, and kept live MLS search disabled.
- Rebuilt `/vision` as a no-account deterministic workspace with editable assumptions, three cases, payment sensitivity and a useful preview before contact collection.
- Added a validated, origin-checked, rate-limited, Turnstile-protected report request API that recomputes all arithmetic server-side.
- Added an additive Supabase migration for an atomic, idempotent lead/consent/attribution/Vision/report/outbox operation and verified it against local PostgreSQL 17.
- Replaced the admin lead placeholder with an RLS-scoped masked list and operations/admin Vision lifecycle context.
- Changed the default live MLS mode from fixture to disabled; the planning demo is a separate non-publishable surface.

## Verification

- `pnpm check`: pass, 172 tests.
- `pnpm db:verify`: pass, including retry idempotency, three touches and all-table RLS.
- `pnpm test:e2e`: 64/64 pass across desktop and mobile.
- `pnpm cf:build`: pass.
- OpenNext preview: 50/50 route smoke pass; known property detail 200; unknown detail 404; generated image 200.
- Wrangler dry run: pass; no deployment.
- Secret-pattern scan and diff check: pass.

## External state

- No production data, remote migrations, RLS, Auth, Storage, credentials, Vercel settings or Cloudflare deployment were changed.
- The public Worker remains on the pre-recovery deployment.
- Production persistence is still blocked on identifying and configuring the real TRACT Supabase project.

## Next

Phase 2: mortgage planner, calculator save/send behavior, first-party lead attribution upgrades, exact lead retry idempotency, CRM fixture trace and richer admin lifecycle evidence.
