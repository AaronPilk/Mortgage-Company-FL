NEXT_PUBLIC_SITE_URL=https://mortgage-company-fl.aaron-9c3.workers.dev
NEXT_PUBLIC_BRAND_NAME=TRACT Mortgage

# Supabase public client configuration. Both values are public by design: the
# URL is the project's API endpoint and the publishable key is built to ship in
# the browser — Row Level Security is the actual access control, and this
# project's RLS posture is asserted by 123 tests plus the hosted hardening
# migrations. The service-role key is a SECRET, lives only in the Worker's
# encrypted settings, and must never appear in this file; the deploy-build
# guard fails the build if any non-NEXT_PUBLIC_ key lands here.
NEXT_PUBLIC_SUPABASE_URL=https://pywikpvjovaayidxyoii.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_lZILsz0xGLBVJXo86_zZRA_4tRy757O
<<<<<<< HEAD

# Turnstile browser widget. The site key is public by design — it renders in
# the page markup for every visitor. Its secret counterpart lives only in the
# Worker's encrypted settings as TURNSTILE_SECRET_KEY.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAEULP2terrAiwi0j
=======
>>>>>>> Provision the production database, and close the gap it shipped with
